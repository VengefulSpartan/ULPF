# Baseline detector on synthetic days

Generated by `python scripts/evaluate_baseline.py`; rerun it to reproduce every number here.

**These are synthetic days**: 24 h of FortiGate logs from 30 internal hosts, internet scan noise and 25 VPN users, with eight attacks injected at known times (scripts/synthetic_network.py). They show what the detector catches and misses when the network's behaviour is known. They do not show its false-flag rate on a real network, which is unknown until it runs on one: the benign traffic here comes from steady random processes, and real traffic is burstier.

Seeds 1, 2, 3. Every line went through the real pipeline (archive, FortiGate pack, OCSF, hash chain); the features were computed from the stored events; each flag was written back through the same writer as a Detection Finding. Windows in the first 2 h are not scored. Settings: threshold 3.5, own history >= 8 windows, peers >= 30, lookback 24 h, minimum increases as in `baseline.MIN_EXCESS`, all fixed before the first run.

## Attacks

| Attack | What was injected | Detected | Time to detection* | What the flag said (seed 1) |
|---|---|---|---|---|
| fast port sweep | 80 ports in 90 s from an address never seen before | 3/3 | 3.0 min | Unusual number of destination ports and denied connections from 203.0.113.66: 80 denied connections in 5 min; all peers' 5473 earlier windows: median 0, highest 4; 80 destination ports in 5 min; all peers' 5473 earlier windows: median 2, highest 7, robust z 52.6 |
| VPN brute force | 40 failed logins as admin in 4 min, then a success | 3/3 | 4.0 min | Unusual failed logins from 192.0.2.77: 40 failed logins in 5 min; all peers' 5885 earlier windows: median 0, highest 2 |
| password spray | one failed login for each of 20 users | 3/3 | 3.0 min | Unusual failed logins from 192.0.2.88: 15 failed logins in 5 min; all peers' 6098 earlier windows: median 0, highest 40 |
| exfiltration burst | ~800 MB to a new address in 10 min | 3/3 | 2.0 min | Unusual data sent from 10.10.2.23: 160.0 MB sent in 5 min; its 176 earlier windows: median 57.5 kB, highest 340.4 kB, robust z 2484.4 |
| lateral SMB scan | 60 internal hosts on 445 in 3 min | 3/3 | 4.0 min | Unusual number of destinations and denied connections from 10.10.1.21: 20 denied connections in 5 min; its 189 earlier windows: median 0, highest 1; 62 destination addresses in 5 min; its 189 earlier windows: median 5, highest 16, robust z 12.8 |
| distributed scan | 300 addresses, one probe each, in 4 min | 3/3 | 4.0 min | Unusual denied connections at device FGT-HQ: 333 denied connections in 5 min; its 224 earlier windows: median 17, highest 108, robust z 53.3 |
| slow port scan | 2-3 ports per 5 min, under the 15-port minimum | 0/3 (expected miss) | — | no flag |
| slow exfiltration | ~20 MB per 5 min, under the 50 MB minimum | 0/3 (expected miss) | — | no flag |

\* From the attack's first line to the end of the first flagged window: when the window closes, the flag can be computed. Run live (`BASELINE_EVERY_MINUTES=5`), add up to 30 s of grace and the run itself.

The two expected misses stay under the smallest increase the detector flags in every 5-minute window (15 ports, 50 MB), which is the point of including them: a per-window baseline does not see an attack spread thin over hours. Catching them needs longer windows or a detector that accumulates over time.

## False flags

Flags that match no injected attack (entity and time).

| Seed | Entity-windows scored | Not scored (no history yet) | False flags | Per 1,000 scored | Which |
|---|---|---|---|---|---|
| 1 | 9,058 | 27 | 1 | 0.11 | Unusual data sent from 10.10.3.5 at 20:00 (the nightly backup, ~300 MB over SSH) |
| 2 | 9,321 | 26 | 1 | 0.11 | Unusual data sent from 10.10.3.5 at 20:00 (the nightly backup, ~300 MB over SSH) |
| 3 | 9,185 | 22 | 1 | 0.11 | Unusual data sent from 10.10.3.5 at 20:00 (the nightly backup, ~300 MB over SSH) |

The nightly backup is benign and is flagged: with 24 hours of history, a once-a-day 300 MB upload has never been seen before. A person marks it expected; a longer lookback or a per-hour-of-day baseline would learn it.

## What was written, checked

| Seed | Lines | Findings written | Invalid OCSF | Chain | Evidence events found, raw hash matching | Second run wrote | Seconds: ingest / read events / features and scores |
|---|---|---|---|---|---|---|---|
| 1 | 32,142 | 11 | 0 | verified, 32,153 records | 289/289 | 0 | 19.4 / 3.7 / 0.8 |
| 2 | 34,845 | 11 | 0 | verified, 34,856 records | 289/289 | 0 | 21.8 / 4.1 / 0.8 |
| 3 | 36,067 | 11 | 0 | verified, 36,078 records | 298/298 | 0 | 23.3 / 4.0 / 0.8 |

Live path, seed 1: replaying 13:00-20:00 one hour at a time as the scheduler does gave 10 flags, one pass over the day gave 10 for the same hours: identical. A window is scored from its own events and earlier ones only, so when it is scored does not change the answer.
