fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.2.0
requests>=2.31.0
python-multipart>=0.0.9
pytest>=8.0.0
httpx>=0.27.0
pyyaml>=6.0
# Kafka input and output connectors
kafka-python-ng>=2.2.2
# Optional: Parquet output (data lakes, Amazon Security Lake custom sources)
#   pyarrow>=15.0
# Audit report PDF export
reportlab>=4.0
orjson>=3.9          # serialises and reads the events; the pipeline falls back to json without it
