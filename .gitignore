# Secrets (keep the template)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
venv/
.venv/

# Runtime data: database, raw archive, device logs
data/*.db
data/*.db-shm
data/*.db-wal
# Connector runtime output: OCSF exports, dead letters, overflow spool, file-tail offsets
data/export/
data/dead_letter/
data/spool/
data/state/
.data/
suricata-logs/

# Parser packs generated by test runs
core/plugins/plugin_fw_test_*.yaml

# Frontend dependencies and build output
ui/node_modules/
ui/dist/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Real third-party sample logs, fetched by scripts/evaluate_public_samples.py (their owners' terms; never committed)
data/public_samples/

# scripts/run_all_checks.py writes one folder per run here
results/
